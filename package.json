{
  "name": "zax-utils",
  "private": false,
  "version": "0.2.1",
  "type": "module",
  "types": "lib/main.d.ts",
  "main": "dist/zax-utils.cjs",
  "module": "dist/zax-utils.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsc -p tsconfig.build.json",
    "preview": "vite preview",
    "git": "npm run build && git add . && git commit -m",
    "postgit": "npm version minor && npm publish && git push --all"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}
